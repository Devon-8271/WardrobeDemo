# TODO — 2026-05-15

## 后端（全部完成 ✅）

| 模块 | 文件 | 状态 |
|---|---|---|
| DB schema | `db.py` | ✅ look表 / style_tags / image_crop_url |
| 多套推荐 | `outfit_recommender.py` | ✅ |
| 拼图生成+裁切 | `outfit_generator.py` | ✅ |
| 穿搭日志 | `look_manager.py` | ✅ |
| 风格档案 | `style_identity.py` | ✅ |
| 意图路由 | `fashion_router.py` | ✅ |
| Dispatch层 | `fashion_dispatch.py` | ✅ |
| image2客户端 | `image2_client.py` | ✅ IP=192.168.31.50 |

---

## Web 前端（进行中）

### 已完成
- [x] `templates/base.html` — 共享 CSS tokens + 组件类 + Jinja2 blocks
- [x] `templates/index.html` — 重构为继承 base.html（聊天界面）

### 待完成

#### 1. `wardrobe.html` — 衣橱浏览页（已在 IDE 打开，待写）
- 继承 `base.html`
- 顶部分类 Tab：全部 / 上装 / 下装 / 外套 / 鞋履 / 配件
- 2列网格 `.card-grid`，每张卡片：图片 + 品类 + 颜色标签
- 右上角"+"按钮 → 跳转 `/wardrobe/upload`
- 调用 `GET /api/wardrobe?category=xxx`
- 右滑或长按删除单品（可选）

#### 2. `looks.html` — 穿搭日志页（待新建）
- 继承 `base.html`
- 顶部场景 Tab：全部 / 日常 / 工作 / 约会 / 运动
- 两种卡片：
  - **OOTD卡**（有图）：效果图封面 + 日期
  - **单品拼图卡**（无图）：白底单品横排拼图 + 日期
- 调用 `GET /api/looks?scene=xxx`（⚠️ 此路由尚未在 app.py 中添加）

#### 3. `app.py` 补充路由（待添加）
```python
GET /api/looks          → db.get_looks() → 返回 JSON
```

---

## Stream B — CLIP 检索（暂缓）
- `clip_matcher.py` 待真实衣橱数据后启动
- 依赖：`clip_retrieval_test.py` 重构

---

## 开会讨论点

1. **AI Look 保存时机** — v1.1 暂不做，默认存单品拼图卡，AI Look 从 Looks 页触发留 v1.2
2. **Hub 推送是否含横滑 outfit 卡片** — 预生成图复用，待确认 Hub 层是否支持横滑交互

---

## 技术备忘

- image2 服务 IP：`http://192.168.31.50:8787`（需同一 WiFi：192.168.31.x）
- Token：`78f0d77eda6b4bbb8927b513d37f51e61ca8e953780a7431`
- 启动命令：`cd 5.14MVP && uvicorn app:app --reload --port 8000`
- VPN 开启时用直接 IP 访问，不走域名（apnetwork TUN 会拦截 DNS）
